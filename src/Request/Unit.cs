namespace EasyRequestHandler.Request
{
    /// <summary>
    /// Represents a unit of work or a no-response scenario. This is typically used as a return type for handlers that do not need to return a value.
    /// </summary>
    public struct Unit
    {
    }
}
